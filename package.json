{
  "name": "whybit-web",
  "private": true,
  "version": "0.0.0",
  "description": "whybit.in — a no-build static site. This manifest exists so Cloudflare Workers Builds has something to install, and to pin wrangler.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
